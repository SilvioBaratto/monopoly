name: monopoly
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy>=1.26
      - pyyaml>=6.0
      - hypothesis>=6.100
      - pytest>=8.0
      - pytest-cov>=5.0
